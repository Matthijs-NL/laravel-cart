name: Release
on:
  push:
    branches: [main]

jobs:
  release:
    uses: Matthijs-NL/general-workflows/.github/workflows/semantic-release.yml@main
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
      config_pat: ${{ secrets.WORKFLOWS_PAT }}
